the first GA run using ode15s, with the new objective function which just uses the mean. This was done on the 16_8 dataset, all of it. It actually terminated, and at a lower obj function value than seen before.

Done on 30_7_2015
